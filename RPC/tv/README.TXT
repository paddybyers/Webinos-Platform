tv module rpc implementation
temporary location until folder structure is set up

DO THIS:
open your chrome browser twice
go on each to location http://localhost/tv/consumer/client.html
on each "Find Service"
on one "addEventListener: channelchange"
on other do "getTvSources" and "setChannel"

